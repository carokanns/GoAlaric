package util

// ASSERT is asserting message f cond is false
func ASSERT(cond bool, msg ...interface{}) {

	//	if !cond {
	//		if len(msg) > 0 {
	//			panic("assert failed. ")
	//		} else {
	//			panic("assert failed. ")
	//		}
	//	}
}
